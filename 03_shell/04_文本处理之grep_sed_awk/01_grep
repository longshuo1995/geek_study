grep 过滤器
sed 流编辑器
awk 报告生成器


grep 与  egrep  egrep 是扩展

第一种格式 grep [option] [pattern] [file1,file2]
option:
    -v 不包含
    -i 忽略大小写
    -n 显示行号
    -E 支持拓展扩展  等价于 egrep
    -F 不支持表达式（当字符处理）

    -c 只输出数量
    -l 只列出文件名,不显示具体内容
    -w 只匹配整词
    -x 只匹配整行
